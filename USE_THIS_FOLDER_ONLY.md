# Use This Folder Only

For Paper 1 submission and handoff, use:

`paper1/`

The older temporary folder `paper1_prediction_outputs/` has been removed to avoid confusion.

## What Is Inside

- `scripts/`: copied Paper 1 scripts.
- `data/`: copied raw dataset, existing Mistral feature file, and cached RoBERTa feature file.
- `model/final_locked_run/`: final cleaned locked results, prediction files, figures, schema audit, metadata, and logs.
- `supplementary/`: Supplementary Material S1 in `.docx` and `.md`.
- `github_repo_draft/`: starter files for the public GitHub repository.

Use `model/final_locked_run/` for manuscript tables and figures.
